use /emil-design-eng and /pbakaus/impeccable to help with this task.

Redesign ALL the hero stat banners at the top of every page in index.html to look more like Tradezella's stat bars — premium, visual, data-rich. Currently they just show big numbers with labels. Upgrade them to have visual elements alongside the numbers.

DESIGN REFERENCE — Each stat should have a visual component, not just a number:
- P&L stat: small sparkline/equity mini-chart to the left of the number (like Tradezella's net cumulative P&L card with the tiny green area chart)
- Win Rate stat: a circular gauge/donut ring showing the percentage visually (half filled = 50%, etc) — two colors: green arc for wins, red arc for losses, with W count and L count shown below
- Avg R:R or Avg Winner: show a horizontal bar split — green portion = avg win size, red portion = avg loss size, proportional lengths
- Profit Factor: a gauge/meter arc (like a speedometer) — needle or filled arc, green zone above 1.0, red below
- Max Drawdown or Total R: clean number with a subtle red downward arrow icon and the peak→trough label

SPECIFIC VISUAL UPGRADES per stat card:

1. P&L card — left side: tiny 60x30px SVG sparkline of cumulative equity (pull from state.trades, draw as a simple polyline, green if positive, red if negative). Right side: the big dollar number + "X trades" subtitle

2. Win Rate card — circular donut ring (SVG, 44px diameter), green arc = win%, red arc = loss%, center shows the % number. Below: "XW / XL" in small muted text

3. Avg Winner R card — two stacked horizontal bars: top bar green showing avg win R length, bottom bar red showing avg loss R length, proportional. Number shown to the right. Label "avg loser XR" below

4. Profit Factor card — semi-circle gauge arc (SVG), filled from left, green past the 1.0 midpoint marker, red before it. Number centered below arc. Label "gross P / gross L"

5. Max Drawdown card — just the red number with a downward chevron icon, "peak→trough" label. Keep simple.

LAYOUT — Keep the same dark gradient banner container with the canvas orb animation behind it. The stat cards sit in a horizontal row inside it. Each card should be:
- Separated by a subtle vertical divider line (1px rgba(255,255,255,0.07))
- Padding 20px 28px
- The visual element on top or left, number prominent below/right
- Labels in small uppercase muted text

This applies to ALL page hero banners:
- Dashboard (Total P&L, Win Rate, Avg Win, Avg R:R, Max Drawdown)
- Stats page (Setups Tracked, Best Win%, Best Setup P&L, Avg R:R)
- History page (Total Trades, Win Rate, Total P&L, Avg R:R)
- Prop Firms page (Combined P&L, Active Accounts, Total Payouts, Net Profit)

Each banner should pull REAL data from state.trades using the existing calcPnL(), calcRR(), periodFilter() functions — same data as before, just with visual elements added.

Make the SVG visuals crisp and minimal — not chunky or decorative. Think Tradezella: clean, functional, the visual IS the data. Use strokeLinecap="round", smooth curves, consistent stroke widths (2-3px for lines, 6-8px for gauge arcs).